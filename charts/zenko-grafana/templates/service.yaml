apiVersion: v1
kind: Service
metadata:
  name: {{ template "zenko-grafana.fullname" . }}
  labels:
    app: {{ template "zenko-grafana.name" . }}
    chart: {{ template "zenko-grafana.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "zenko-grafana.name" . }}
    release: {{ .Release.Name }}
