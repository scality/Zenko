---
# DO NOT EDIT - autogenerated file
apiVersion: zenko.io/v1alpha1
kind: ZenkoVersion
metadata:
  name: '${ZENKO_VERSION_NAME}'
spec:
  dashboards:
    #cloudserver:
    #  image: '${CLOUDSERVER_DASHBOARD}'
    #  tag: '${CLOUDSERVER_TAG}'
    kafka:
      image: kafka_grafana_dashboard
      tag: '${ZENKO_VERSION_NAME}'
    mongodb:
      image: mongodb_grafana_dashboard
      tag: '${ZENKO_VERSION_NAME}'
    redis:
      image: redis_grafana_dashboard
      tag: '${ZENKO_VERSION_NAME}'
    #vault:
    #  image: '${VAULT_DASHBOARD}'
    #  tag: '${VAULT_TAG}'
    zookeeper:
      image: zookeeper_grafana_dashboard
      tag: '${ZENKO_VERSION_NAME}'
  versions:
    management:
      ui:
        image: '${ZENKO_UI_IMAGE}'
        tag: '${ZENKO_UI_TAG}'
      api:
        image: '${PENSIEVE_API_IMAGE}'
        tag: '${PENSIEVE_API_TAG}'
      pushAPI:
        image: '${PENSIEVE_API_IMAGE}'
        tag: '${PENSIEVE_API_TAG}'
    cloudserver:
      image: '${CLOUDSERVER_IMAGE}' 
      tag: '${CLOUDSERVER_TAG}'
    backbeat:
      image: '${BACKBEAT_IMAGE}' 
      tag: '${BACKBEAT_TAG}'
    blobserver:
      image: '${BLOBSERVER_IMAGE}' 
      tag: '${BLOBSERVER_TAG}'
    jabba:
      image: '${JABBA_IMAGE}' 
      tag: '${JABBA_TAG}'
    utapi:
      image: '${UTAPI_IMAGE}' 
      tag: '${UTAPI_TAG}'
    secureChannelProxy:
      image: '${CLOUDSERVER_IMAGE}' 
      tag: '${CLOUDSERVER_TAG}'
    localData:
      image: '${CLOUDSERVER_IMAGE}' 
      tag: '${CLOUDSERVER_TAG}'
    metrics:
      image: '${CLOUDSERVER_IMAGE}' 
      tag: '${CLOUDSERVER_TAG}'
    s3utils:
      image: '${S3UTILS_IMAGE}' 
      tag: '${S3UTILS_TAG}'
    zookeeper:
      image: '${ZOOKEEPER_IMAGE}' 
      tag: '${ZOOKEEPER_TAG}'
    kafka:
      cluster:
        image: '${KAFKA_IMAGE}' 
        tag: '${KAFKA_TAG}'
      cruiseControl:
        image: '${KAFKA_CRUISECONTROL_IMAGE}'
        tag: '${KAFKA_CRUISECONTROL_TAG}'
      monitoring:
        image: '${JMX_JAVAAGENT_IMAGE}'
        tag: '${JMX_JAVAAGENT_TAG}'
    vault:
      image: '${VAULT_IMAGE}'
      tag: '${VAULT_TAG}'
    shell:
      image: '${BUSYBOX_IMAGE}'
      tag: '${BUSYBOX_TAG}'
    mongodb: '${MONGODB_TAG}'
    redis:
      db:
        image: '${REDIS_IMAGE}'
        tag: '${REDIS_TAG}'
      exporter:
        image: '${REDIS_EXPORTER_IMAGE}'
        tag: '${REDIS_EXPORTER_TAG}'
      kubedb: '${REDIS_KUBEDB_TAG}'
